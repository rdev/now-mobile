import React from 'react';
import * as Animatable from 'react-native-animatable';
import { connect } from '../Provider';

const NetworkError = ({ context }) => {};

export default connect(NetworkError);
